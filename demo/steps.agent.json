{
  "type": "cli",
  "theme": "rose-pine-dawn",
  "font_size": 18,
  "width": 1200,
  "height": 860,
  "typing_speed_ms": 45,
  "output": { "gif": "agent.gif", "mp4": "agent.mp4" },
  "require": ["claude", "varve"],
  "setup": [
    "export VARVE_STORE=/tmp/varve-demo/acme-context",
    "cd /tmp/varve-demo/atlas-web",
    "clear"
  ],
  "cleanup": [],
  "steps": [
    { "cmd": "# end of a session. you do not write the log — the agent does.", "pause_ms": 1800 },
    { "cmd": "claude -p --add-dir $VARVE_STORE --permission-mode acceptEdits 'Use varve-publish. We kept the 419 expiry response instead of reverting to 401 — mobile already ships against 419. atlas-api deploys before atlas-web. Write the log, do not push, then reply with one short line.'", "pause_ms": 4000 },
    { "cmd": "varve", "wait": "varve-load", "pause_ms": 3500 }
  ]
}
